命名约定是标识符要遵循的一组规则。

它使得代码对于其他程序员也更可读和可理解。

Box中的命名约定遵循标准JavaScript约定，这不是强制性的，而是遵循的良好做法。

它应该遵循camel case语法命名类，方法，变量和属性。

如果name与两个单词组合，则第二个单词将始终以大写字母开头。 doLayout（），StudentForm，firstName等。


- Class Name
最开始加上该应用的名称,之后用'.'隔开,类似于命名空间,它应该以小写字母开头，然后是camel case E.g. HY.request.studentClass
- Method Name
它应该以小写字母开头，然后是camel case E.g. doLayout()
如果是自己内部使用的方法,不想给外部调用,建议用'_'打头  E.g _open()
如果是提供给外部调用的,建议用'fn'打头,  E.g  fnClose()
- Variable Name
它应该以小写字母开头，然后是camel case E.g. firstName
- Constant Name
它应该是只有大写，例如 E.g. COUNT, MAX_VALUE
- Property Name
它应该以小写字母开头，然后是camel case e.g. enableColumnResize = true